# This file is PLAIN TEXT and only for reference — it documents the shape
# `secrets/secrets.yaml` needs. It is never read by any Nix module (only
# `secrets/secrets.yaml`, which doesn't exist in this repo, is).
#
# Do not fill this file in and rename it — create the real thing with
# `sops secrets/secrets.yaml`, which encrypts it as you save. See
# README → "Secrets (sops-nix)" for the exact steps.

mail:
    imap_password: "your TU Berlin / app-specific IMAP password goes here"
